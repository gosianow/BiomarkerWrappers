% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers_cox_regression.R
\name{wrapper_cox_regression_core_simple}
\alias{wrapper_cox_regression_core_simple}
\alias{wrapper_cox_regression_core_simple_strat}
\title{Cox regression with simple additive model}
\usage{
wrapper_cox_regression_core_simple(
  data,
  tte_var,
  censor_var,
  covariate_vars,
  strata_vars = NULL,
  return_vars = NULL,
  keep_obs = TRUE,
  variable_names = NULL,
  caption = NULL,
  force_empty_cols = FALSE,
  sr_times = NULL,
  print_nevent = TRUE,
  print_mst = TRUE,
  print_total = TRUE,
  print_pvalues = TRUE,
  print_adjpvalues = TRUE,
  print_hr = TRUE
)

wrapper_cox_regression_core_simple_strat(
  data,
  tte_var,
  censor_var,
  covariate_vars,
  strata_vars = NULL,
  return_vars = NULL,
  strat1_var = NULL,
  strat2_var = NULL,
  variable_names = NULL,
  caption = NULL,
  force_empty_cols = FALSE,
  sr_times = NULL,
  print_nevent = TRUE,
  print_mst = TRUE,
  print_total = TRUE,
  print_pvalues = TRUE,
  print_adjpvalues = TRUE,
  print_hr = TRUE
)
}
\arguments{
\item{data}{Data frame.}

\item{tte_var}{Name of the time-to-event variable. This variable must be numeric.}

\item{censor_var}{Name of the censor variable. It has to be numeric and encode 1 for event and 0 for censor.}

\item{covariate_vars}{Vector with names of covariates that are included in the formula of the simple additive model: ~ covariate_vars[1] + covariate_vars[2] + covariate_vars[3] + ....}

\item{strata_vars}{Vector with names of covariates that are used as strata.}

\item{return_vars}{Vector with names of covariates for which the statistics should be returned. If NULL, statistics are returned for all covariates.}

\item{variable_names}{Named vector with variable names. If not supplied, variable names are created by replacing in column names underscores with spaces.}

\item{caption}{Caption for the table with results.}

\item{force_empty_cols}{Logical. Whether to display output columns which are all empty.}

\item{sr_times}{Vector of times used to compute survival rates.}

\item{print_nevent}{Logical. Whether to print numbers of events.}

\item{print_mst}{Logical. Whether to print median survival time (MST).}

\item{print_total}{Logical. Whether to print total number of samples and total number of events.}

\item{print_pvalues}{Logical. Whether to print p-values.}

\item{print_adjpvalues}{Logical. Whether to print adjusted p-values.}

\item{strat1_var}{Name of the first stratification variable used for splitting the data.}

\item{strat2_var}{Name of the second stratification variable used for splitting the data.}
}
\description{
This function can be used if one is interested in effects of multiple covariates in a multivariate model.
}
\details{
If for a factor covariate that should be returned the reference level has zero count, results are set to NAs because this levels is not used as a reference which means that it is not possible to fit the model that we want.
}
\examples{

data(bdata)

data <- bdata
tte_var <- "PFS"
censor_var <- "PFS_Event"
covariate_vars <- c("Treatment_Arm", "GeneA", "IPI", "Cell_Of_Origin")

x <- wrapper_cox_regression_core_simple(data, tte_var = tte_var, censor_var = censor_var, covariate_vars = covariate_vars)

boutput(x)

bforest(x)


### Fit a stratified model 

covariate_vars <- c("Treatment_Arm", "GeneA")
strata_vars <- c("IPI", "Cell_Of_Origin")

x <- wrapper_cox_regression_core_simple(data, tte_var = tte_var, censor_var = censor_var, covariate_vars = covariate_vars, strata_vars = strata_vars)

boutput(x)



data(bdata)

data <- bdata
tte_var <- "PFS"
censor_var <- "PFS_Event"
covariate_vars <- c("IPI", "GeneA")

strat1_var = "Cell_Of_Origin"
strat2_var = "Treatment_Arm"


x <- wrapper_cox_regression_core_simple_strat(data, tte_var = tte_var, censor_var = censor_var, covariate_vars = covariate_vars, strat1_var = strat1_var, strat2_var = strat2_var)

boutput(x)

}
